import { VoyDetaileDirective } from './voy-detaile.directive';

describe('VoyDetaileDirective', () => {
  it('should create an instance', () => {
    const directive = new VoyDetaileDirective();
    expect(directive).toBeTruthy();
  });
});
